#!/bin/bash
# 1. utilities for launching multiple terminals
sudo apt install -y xterm
# 2. ROS navigation stack:
sudo apt install ros-kinetic-navigation
# 3. python-notify
sudo apt install python-notify